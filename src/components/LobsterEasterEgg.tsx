import { useState } from 'react'
export default function LobsterEasterEgg() { const [clicks, setClicks] = useState(0); const [running, setRunning] = useState(false); const [party, setParty] = useState(false); const activate = () => { const next = clicks + 1; setClicks(next); setRunning(true); window.setTimeout(() => setRunning(false), 1700); if (next >= 5) { setParty(true); setClicks(0); window.setTimeout(() => setParty(false), 2200) } }; return <><button type="button" className={`lobster-trigger ${running ? 'running' : ''}`} onClick={activate} aria-label="A mysterious terminal visitor">🦞</button>{running && <div className="lobster-log">&gt; OPENCLAW DETECTED<br />&gt; AGENT ESCAPED FROM TERMINAL<br />&gt; 🦞🦞🦞</div>}{party && <div className="lobster-party" aria-hidden="true">{Array.from({ length: 24 }, (_, index) => <span key={index} style={{ '--i': index } as React.CSSProperties}>🦞</span>)}</div>}</> }
